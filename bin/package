#!/bin/bash

set -eu
set -o pipefail

cd "$(dirname "$0")/.."

tag="$(git describe --exact-match)"
version="${tag#v}"
name="A4s_Pricemotion-$version"

mkdir -p dist/
git archive -o dist/"$name".tgz HEAD package.xml app
